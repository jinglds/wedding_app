<%= search_form_for @q do |f| %>

  # Search if the name field contains...
  <%= f.label :name_cont %>
  <%= f.search_field :name_cont %>
  <%= f.select :shop_type_eq, options_for_select(Shop.uniq.pluck(:shop_type)), prompt: "Select Vendor Type", :selected => params[:pid] %>


  <%= f.submit %>
<% end %>


