<% provide(:title, @event.name) %>
<%= render 'events/heading' %>
<div class="container">
<div class="row">
	<%= render 'events/event_menu' %>
</div>
<div class="container">
	<h2>Edit Expense</h2>
	<div class="form-all col-md-4 col-md-offset-4">
		<%= render 'expenses/expense_form' %>
		<%= link_to "< Back to Expenses", event_expenses_path(@event) %>
	</div>
</div>