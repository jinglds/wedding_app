
	<%= checklist.title %>
	<%= checklist.time_range %> | 
	<%= link_to "Add to task", new_event_task_path(@event, :checklist_id=>checklist.id) %> <%= image_tag("add_task_icon_grey.png",  :width=>18, :height=>18, :border => 0, :alt => "add task icon") %> 
	<% if checklist.completed == false%>
		<%= link_to "Complete", event_checklist_complete_path(@event, checklist), method: :put%>
		<%= image_tag("done_icon_grey.png",  :border => 0, :alt => "done icon") %>
	<%else%>
		<%= link_to "DeComplete", event_checklist_decomplete_path(@event, checklist), method: :put%>
		<%= image_tag("undo_icon_grey.png",  :width=>18, :height=>18, :border => 0, :alt => "undo icon") %>
	<%end%>

