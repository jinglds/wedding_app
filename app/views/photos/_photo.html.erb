<% @photos.each do |p| %>
  <%= image_tag p.image_url %>

  <% if (current_user==@shop.user) %>
	  <%= link_to "Edit", edit_shop_photo_path(@shop, p) %>
	  <%= link_to "Delete", shop_photo_path(@shop, p.id), method: :delete,
	                                     data: { confirm: "You sure?" } %>
	<% end %>
<% end %>