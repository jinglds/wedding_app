<% provide(:title, @event.name) %>
<%= render 'events/heading' %>
<div class="container">
<div class="row">
	<%= render 'events/event_menu' %>
</div>

<div class="container">
	<div class="row">
		<div class="col-md-6 col-md-offset-3">
			<h2>Edit Task</h2>
			<%= render 'tasks/task_form' %>
		<%= link_to "<Back to Tasks", event_tasks_path(@event) %>
		</div>
	</div>

</div>