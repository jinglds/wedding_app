<% provide(:title, @event.name) %>
<%= render 'events/heading' %>
<div class="container">
<div class="row">
	<%= render 'events/event_menu' %>
</div>
<div class="row">
	<div class="col-md-6 col-md-offset-3">

	<h2>New Task</h2>
	<%= render @task.parent if @task.parent %>
	<%= render 'tasks/task_form' %>
</div>

</div>